'use strict';
const {app, assert} = require('egg-mock/bootstrap');

describe('create()', () => {
	// it('should create user', async () => {
	// 	const ctx = app.mockContext();
	// 	const data = {
	// 		username: 'lin_test1',
	// 		password: '123'
	// 	};
	// 	const user = await ctx.service.user.create(data);
	// 	assert(user);
	// });


});